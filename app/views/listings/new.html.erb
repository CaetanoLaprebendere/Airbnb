
  <h2><%= t(".title") %></h2>

  <%= form_for [current_user, @listing] do |form| %>
    <%= render partial: '/listings/form', object: form %>

    <div class="submit-field">
      <%= form.submit %>
    </div>
  <% end %>

